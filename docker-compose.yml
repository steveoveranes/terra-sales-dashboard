services:
  dashboard:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    env_file: .env
    environment:
      - DATA_DIR=/data
      - PORT=8080
    volumes:
      - dashboard-data:/data
    restart: unless-stopped

volumes:
  dashboard-data:
